from setuptools import setup, find_packages

setup(
    name='jhamon_training',
    version='0.1.0', # You might want to adjust this
    packages=find_packages(),
    # Add other dependencies here if known, e.g.,
    # install_requires=[
    #     'pandas',
    #     'numpy',
    #     'matplotlib',
    #     'feather-format', # or 'pyarrow' if used for feather
    # ],
)
